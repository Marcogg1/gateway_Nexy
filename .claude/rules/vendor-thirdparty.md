---
paths:
  - "vendor/**"
  - "typings/**"
---

These are third-party sources (`vendor/nexyhub-ble-sdk`) and type stubs (`typings/azure`).
Do not hand-edit them. Fix upstream / regenerate stubs, or wrap in our own module under `src/`.
